# 将棋NEXT — ローグライト将棋 設計書

作成日: 2026-07-19 / ステータス: 承認済み設計(実装計画はこの後 writing-plans で作成)

## 1. コンセプト

[将棋ライク](https://unityroom.com/games/shougi-like)にインスパイアされたローグライト将棋。
通常の将棋(9x9固定盤・初期配置40枚)をベースに、特殊能力を持つ追加駒を獲得しながら全15面を勝ち進み、最終ボスの撃破を目指す。ブラウザで動作する。

将棋ライクとの意図的な差分:

- 盤面は常に9x9固定(将棋ライクは可変)
- HP/攻撃力制ではなく**純粋な将棋ルール**(利きに入った駒は即取り)
- 持ち駒ルールあり(通常駒のみ)

## 2. 確定済みの意思決定

| 項目 | 決定 |
|---|---|
| プラットフォーム | ブラウザゲーム。Vite + TypeScript(strict)、ランタイム依存ゼロ |
| 戦闘ルール | 純粋な将棋ルール(即取り)。HP等はなし |
| 持ち駒 | 通常駒(歩香桂銀金角飛)のみ持ち駒化・打ち可。特殊駒は取られたら消滅(例外は駒個別に明記) |
| 編成 | 各面開始前の編成画面で、獲得済み特殊駒を初期配置の通常駒(玉以外)と入れ替え |
| 駒獲得 | 各面クリア後、ランダム3枚から1枚選択(スキップ可) |
| 敗北 | 完全ロスト。獲得駒を全て失い1面から再スタート |
| 初心者モード | ラン開始時にレア/神話レア駒1枚(70%/30%抽選)を編成済みで開始する救済モード(§4.1.1) |
| 敵編成 | 敵も面進行に応じ特殊駒を編成。5/10/15面はプレイヤー入手不可の専用ボス駒を持つボス |
| 勝利条件 | 王(またはボス駒)を実際に取ったら勝ち。王手放置も合法。UIで王手警告 |
| アーキテクチャ | 素のTS+DOM盤。コアは純粋関数、能力はデータ+フック、AIはWeb Workerでミニマックス |

## 3. ゲームルール詳細

### 3.1 基本

- 盤は9x9。両軍とも通常将棋の初期配置(40枚)を基本とし、編成画面での入れ替えのみ配置が変わる。
- 手番はプレイヤー先手。交互に1手ずつ。
- 勝敗条件: 相手の**ロイヤル駒**(王・太子・ボス駒)を全て取ったら勝ち。自分のロイヤル駒を全て取られたら負け。両軍対称のルール(敵の酔象が太子に成った場合も同様)。
- 用語: 「隣接」=周囲8マス。「自陣」=自分側の1〜3段目。
- **王手放置・自殺手も合法**。詰み判定は行わない(35種の能力との組み合わせバグを構造的に排除するため)。王手中はUIに警告を表示する。
- 千日手・持将棋の判定はしない。AIの千日手ループは評価関数の微小乱数と進行ボーナスで回避する。

### 3.2 持ち駒と打ち

- 取った**通常駒**は持ち駒になり、通常将棋と同じく空きマスに打てる(成り駒を取ったら元の駒として持つ)。
- 二歩は禁止。行き所のない打ち(最奥段の歩香、最奥2段の桂)は禁止。
- 打ち歩詰めは判定しない(詰み概念がないため。王を取られる側が回避できなければそのまま負け)。
- 取った**特殊駒**は持ち駒にならず消滅する(妖狐など駒個別の例外を除く)。

### 3.3 成り

- 通常将棋準拠: 敵陣3段目に「入る/出る/中で動く」移動の後に成りを選択できる(不成も可)。
- 行き所のなくなる移動は強制成り(歩香桂のルールを特殊駒にも一般化。該当駒は全て成り先を持つよう設計済み)。
- 成り先の記載がない特殊駒は成れない。
- **特殊駒は成っても固有能力を保持する**(動きだけが変わる)。例外: 酔象→太子のように成り自体が能力になっているもの。

## 4. ラン構造

### 4.1 フロー

```
タイトル ─→ モード選択(通常 / 初心者) ─→ ラン開始
             └→ [編成画面 → 対局 → 勝利 → 報酬3択] × 15面 → 15面クリアでラン勝利
敗北 → リザルト(到達面・獲得駒表示) → 全ロスト → タイトルへ
```

### 4.1.1 初心者モード(初心者救済)

- 新規ラン開始時にモードを選ぶ。**初心者モード**では、レア70%/神話レア30%でレアリティを抽選し、そのレアリティから等確率で選んだ特殊駒1枚を**獲得済み+編成済み**の状態でランを開始する。
- 初期セット位置は§5の置換規則に従い自動配置(レア以上=金角飛のいずれかと置換)。1面開始前の編成画面で自由に変更できる。
- それ以外のルール(報酬・敵編成・完全ロスト)は通常モードと同一。リザルトと統計にはモードを記録する。

### 4.2 編成ルール

- 獲得済み特殊駒は「控え」として無制限に所持できる。同一駒の複数所持も可。
- 編成画面で、初期配置の通常駒(玉以外の39枚)と特殊駒をクリックで入れ替える。特殊駒は入れ替えた通常駒の初期位置に置かれる。
- 特殊駒を控えに戻して通常駒に戻すのも自由。編成は面をまたいで記憶される。

### 4.3 報酬抽選

クリアした面に応じたレアリティ確率で、互いに異なる3枚を提示(所持済みの駒も提示され得る)。

| クリア面 | コモン | アンコモン | レア | 神話レア |
|---|---|---|---|---|
| 1〜3 | 70% | 25% | 5% | 0% |
| 4(通常面) | 50% | 35% | 13% | 2% |
| **5(ボス)** | 0% | 0% | **80%** | **20%** |
| 6〜9 | 35% | 35% | 23% | 7% |
| **10(ボス)** | 0% | 0% | **70%** | **30%** |
| 11〜12 | 20% | 35% | 33% | 12% |
| 13〜14 | 10% | 30% | 40% | 20% |
| 15 | ランクリアのため報酬なし | | | |

### 4.4 永続化

- ランの状態(現在面・所持駒・編成・対局中の盤面と手番)を**1手ごと**にlocalStorageへ自動保存。リロードで途中から再開。敗北直前リロードによる巻き戻しはさせない(敗北確定時に即セーブ)。
- 図鑑(遭遇/獲得済み駒)・統計(クリア回数、最高到達面)は別キーで永続。
- キー: `syoginext:run` / `syoginext:codex` / `syoginext:stats`。全て`version`フィールドを持つJSON。

## 5. ステージデータ

敵編成は「通常将棋の初期配置の一部を特殊駒に置換」で表現する。AI深度はミニマックスの探索深さ。

| 面 | AI深度 | 敵の特殊駒(通常駒と置換) |
|---|---|---|
| 1 | 2 | なし(純粋な通常将棋) |
| 2 | 2 | 仲人、鉄将 |
| 3 | 2 | 石将、銅将、犬 |
| 4 | 3 | 弓兵、土竜、木将、旗兵 |
| 5 | 3 | **ボス: 鬼武者**(王と置換)+兎、鉄将、銅将 |
| 6 | 3 | 猛豹、槍兵、仲人、犬 |
| 7 | 3 | 風車、盾兵、怨念、弓兵、土竜 |
| 8 | 3 | 八方桂、隠密、妖狐、猛豹 |
| 9 | 4 | 影武者、槍兵、風車、怨念、兎、旗兵 |
| 10 | 4 | **ボス: 九尾**(王と置換)+猛豹、盾兵、八方桂、妖狐 |
| 11 | 4 | 酔象、磁将、風車、隠密、槍兵 |
| 12 | 4 | 麒麟、狙撃手、石化の魔女、八方桂、盾兵 |
| 13 | 4 | 鳳凰、影の刺客、爆弾兵、猛豹、怨念、妖狐 |
| 14 | 5 | 獅子、磁将、狙撃手、影の刺客、風車、八方桂 |
| 15 | 5 | **ボス: 覇王**(王と置換)+軍神、不死鳥、麒麟、鳳凰、影の刺客 |

- どの通常駒と置換するかは各面の`stages.ts`データで固定定義する。置換規則: コモン級は歩と、アンコモン級は香桂銀と、レア以上は金角飛と置換する(具体的なマスはこの規則の範囲でステージデータ作成時に確定)。
- AI思考時間の目安: 深度2=~0.5秒、深度3=~1秒、深度4=~2秒、深度5=~3秒(αβ+反復深化で時間打ち切り)。

## 6. 駒カタログ

動き表記: 「前1」=前に1マス。「スライダー」=飛角のように何マスでも。「ジャンプ」=間の駒を飛び越す。成り「金」=金と同じ動きになる(能力は保持)。

### 6.1 コモン(10種)

| # | 駒 | 動き | 能力 | 成り |
|---|---|---|---|---|
| C1 | 仲人(ちゅうにん) | 前後1 | — | 金 |
| C2 | 石将 | 斜め前2方向1 | — | 金(強制成りあり) |
| C3 | 鉄将 | 前+斜め前(3方向)1 | — | 金(強制成りあり) |
| C4 | 銅将 | 前3方向+真後ろ1 | — | 金 |
| C5 | 犬 | 前+斜め後ろ(3方向)1 | — | 狼: 前後+斜め前(4方向)1 |
| C6 | 兎 | 前1+斜め前2ジャンプ | 跳び越え可 | 金(強制成りあり) |
| C7 | 弓兵 | 前1、または前2ジャンプ | 跳び越えて捕獲可 | 大弓: 前1〜2ジャンプ+斜め前1+横1(強制成りあり) |
| C8 | 土竜(もぐら) | 前後1 | 生駒の歩・香からは取られない(と金・成香からは取られる) | 金 |
| C9 | 木将 | 斜め4方向1 | — | 金 |
| C10 | 旗兵 | 前+横(3方向)1 | — | 金 |

### 6.2 アンコモン(9種)

| # | 駒 | 動き | 能力 | 成り |
|---|---|---|---|---|
| U1 | 猛豹 | 前後+斜め(6方向)1 | — | **角** |
| U2 | 風車 | 横スライダー+前後1 | — | **飛** |
| U3 | 八方桂 | 桂の8方向版(チェスナイト) | 跳び越え可 | 金 |
| U4 | 槍兵 | 前2まで+横1 | — | 長槍: 前3まで+横1+斜め前1 |
| U5 | 盾兵 | 縦横1 | 正面からの直進移動では取られない(斜め・ジャンプ・横からは取られる) | 金 |
| U6 | 怨念 | 斜め4方向1 | **道連れ**: この駒を取った駒も消滅(王・太子・ボス駒は消滅しない) | なし |
| U7 | 隠密 | 前+斜め(5方向)1 | 1ゲーム1回、手番を消費して自陣(1〜3段)の空きマスへワープ | 金 |
| U8 | 妖狐 | 斜め4方向2まで | 取られたとき消滅せず、**自分の駒台に歩として戻る** | 金 |
| U9 | 影武者 | 8方向1(玉と同じ) | 1ゲーム1回、手番を消費して盤上の味方の王(不在なら太子)と位置を交換 | なし |

### 6.3 レア(8種)

| # | 駒 | 動き | 能力 | 成り |
|---|---|---|---|---|
| R1 | 酔象 | 真後ろ以外7方向1 | — | **太子**: 8方向1。王が取られても太子が盤上にいれば敗北しない(第二の王として扱う) |
| R2 | 麒麟 | 縦横2ジャンプ+斜め1 | — | **獅子**(M1参照) |
| R3 | 鳳凰 | 斜め2ジャンプ+縦横1 | — | **奔王**(M2参照) |
| R4 | 爆弾兵 | 縦横1 | 取られたとき、取った駒とこの駒の周囲8マスの全駒が消滅(敵味方無差別。王・太子・ボス駒は消滅しない。消滅駒は持ち駒にならない) | なし |
| R5 | 磁将 | 8方向1 | 移動後、縦横4方向それぞれの最寄り敵駒(ロイヤル駒除く)から1体選び、磁将の方向へ1マス引き寄せられる(引き寄せ先が空きマスの場合のみ。任意) | なし |
| R6 | 狙撃手 | 斜め4方向1 | 1ゲーム2回、手番を消費して前方直線3マス以内の敵駒を狙撃して消滅させる(王・太子・ボス不可。間に駒があると不可。持ち駒にならない) | なし |
| R7 | 影の刺客 | 斜めスライダー(角) | 敵駒を取ったとき、そのマスから続けてもう1回だけ移動できる(1手番につき追加1回。追加移動でも捕獲可) | なし |
| R8 | 石化の魔女 | 斜め4方向2まで | 移動後、隣接する敵駒1体(王・ボス除く)を選び、相手の次の手番の間その駒を行動不能にできる(任意) | なし |

### 6.4 神話レア(5種)

| # | 駒 | 動き | 能力 | 成り |
|---|---|---|---|---|
| M1 | 獅子 | 8方向1を**1手番に2回まで**(1回目で取ってさらに動ける。元のマスに戻る「居食い」も可) | 中将棋の獅子の簡易版 | なし |
| M2 | 奔王 | 全8方向スライダー(クイーン) | — | なし |
| M3 | 不死鳥 | 斜めスライダー+縦横1 | 取られたとき1度だけ、自陣(1〜3段)のランダムな空きマスに復活(空きがなければ消滅) | なし |
| M4 | 軍神 | 8方向1 | **隣接する味方駒は敵に取られない**(王・太子には効果なし。軍神自身は取られる) | なし |
| M5 | 傀儡師 | 縦横2まで | 1ゲーム1回、手番を消費して隣接する敵の通常駒1体を自軍に寝返らせる(王・特殊駒不可。成り状態は維持) | なし |

### 6.5 敵専用ボス駒(3種) — 敵の王と置換。取れば勝利

| # | 駒 | 面 | 動き | 能力 |
|---|---|---|---|---|
| B1 | 鬼武者 | 5 | 8方向1+前2まで | — |
| B2 | 九尾 | 10 | 8方向2まで | 回避ワープx2: プレイヤーの手番終了時に利きに入っていたら、利きのない隣接空きマスへ自動移動(候補がなければ発動しない) |
| B3 | 覇王 | 15 | 8方向2まで | 回避ワープx3(九尾と同じ)+隣接する配下(味方駒)は取られないオーラ(覇王自身には効果なし) |

計: プレイヤー獲得可能32種(+固有成り形12種: 狼/大弓/長槍/太子/獅子/奔王ほか金成り含む)+ボス3種=**35種**。

## 7. 能力の裁定(エッジケース)

1. **取られない系(土竜・盾兵・軍神オーラ・覇王オーラ)**: 該当する捕獲手はそもそも合法手として生成されない。王の捕獲だけは全ての保護を無視できない=王・太子・ボス駒は保護対象外(§6.4 M4のとおり)ため、「王が取れない」手詰まりは発生しない。
2. **道連れ・爆発と王**: 王・太子・ボス駒はいかなる能力でも消滅しない(取られる時は通常の捕獲のみ)。
3. **爆弾兵の連鎖**: 爆発で別の爆弾兵が消滅した場合、その爆発も連鎖する。爆発で怨念が消えても道連れは発動しない(「取られた」ではないため)。
4. **不死鳥・妖狐 vs 道連れ等**: 「取られた」扱いの能力は捕獲時のみ発動。爆発・狙撃による消滅では発動しない。
5. **ボスの回避ワープ**: プレイヤー手番の完全終了後(能力処理含む)に判定。ワープは1手番につき最大1回消費。
6. **獅子の2段動作**: 1回目の移動で王を取ったら即勝利(2回目は行わない)。獅子が1手で2枚取ることも可。
7. **成りと能力**: 特殊駒は成っても固有能力を保持(§3.3)。金成りした怨念は存在しない(怨念は成れない)ように、成り不可駒は敵陣でもそのまま。
8. **強制成り**: その移動後に動けなくなる駒は強制成り。該当: 石将・鉄将・兎・弓兵(+通常の歩香桂)。
9. **AIと能力**: アクティブ能力(隠密・影武者・狙撃手・傀儡師・磁将・石化)も合法手として生成し、AIも使用する。
10. **石化の裁定**: 石化中の駒は移動・捕獲・能力使用が一切できない(利きなし扱い。王手警告やボスの回避ワープ判定にも数えない)。効果は相手の次の手番終了まで。
11. **報酬抽選の手順**: 3枠それぞれ独立に§4.3の確率でレアリティを抽選し、そのレアリティの駒から未選出のものを等確率で選ぶ(3枚は互いに異なる駒になる)。

## 8. アーキテクチャ

Vite + TypeScript(strict)。ランタイム依存ゼロ。テストはVitest。

```
src/
  core/            # 純粋ロジック(DOM非依存・全てユニットテスト対象)
    types.ts       #   Piece / Board / GameState / Move / RunState 型定義
    board.ts       #   盤操作・利き計算
    moves.ts       #   合法手生成(動きパターン+能力フックによる修正)
    abilities.ts   #   能力フックの実行エンジン
    pieces.ts      #   駒定義データ(通常8種+特殊32種+成り形+ボス3種)
    game.ts        #   1手の適用・勝敗判定・成り処理
    run.ts         #   ラン進行(ステージ・獲得駒・報酬抽選・編成)
    stages.ts      #   15面の敵編成/AI設定データ(§5)
  ai/
    worker.ts      #   Web Workerエントリ(思考中もUIが固まらない)
    search.ts      #   ミニマックス+αβ枝刈り+反復深化+評価関数
  ui/
    screens/       #   タイトル/編成/対局/報酬/図鑑/リザルト
    board-view.ts  #   DOM盤面(9x9グリッド+CSS駒)
  storage.ts       # localStorage永続化(§4.4)
  main.ts
```

### 8.1 駒定義(データ+フック)

エンジン本体は個別の駒を知らない。新駒追加=`pieces.ts`に1エントリ追加で完結する。

```ts
interface PieceDef {
  id: string; name: string; kanji: string; rarity?: Rarity; // 通常駒はrarityなし
  moves: MovePattern[];        // step(1歩) / slide / jump / multiStep(獅子)
  promotesTo?: string;         // 成り先ID。なければ成れない
  isRoyal?: boolean;           // 王・太子・ボス駒
  hooks?: {
    blockCapture?:  (ctx) => boolean;  // 土竜・盾兵・軍神/覇王オーラ
    onCaptured?:    (ctx) => Effect[]; // 怨念・爆弾兵・妖狐・不死鳥
    onCapture?:     (ctx) => Effect[]; // 影の刺客の追加移動
    afterMove?:     (ctx) => Effect[]; // 磁将・石化の魔女(任意効果はUI/AIが選択)
    afterOpponentTurn?: (ctx) => Effect[]; // 九尾・覇王の回避ワープ
    active?:        ActiveAbility;     // 隠密・影武者・狙撃手・傀儡師(1ゲームN回)
  };
}
```

- 全フックは`Effect[]`(駒の消滅・移動・生成・状態付与)を返し、`game.ts`が一括適用する。乱数(不死鳥の復活先など)はシード付きRNGを使い、テストとリプレイを決定的にする。
- 石化などの状態はGameState側の`statusEffects`に持つ(駒定義は不変データ)。

### 8.2 AI

- ミニマックス+αβ枝刈り+反復深化。面ごとの深度と時間上限は§5。
- 評価関数: 駒割(通常駒は本家準拠の点、特殊駒はレアリティと動きの広さで加点)+王(ボス)の安全度+前進・中央支配の位置点+微小乱数(千日手ループ回避)。
- Web Worker内で実行。GameStateはシリアライズして受け渡す(構造化クローン可能なプレーンオブジェクトで設計)。

## 9. UI設計

- **タイトル**: 新規ラン(通常/初心者モード選択) / 続きから(セーブがある時) / 図鑑
- **編成画面**: 自陣3段+控え特殊駒リスト。クリックで入れ替え。駒にホバー/タップで動き図(9x9ミニ図)と能力説明を表示。「出陣」ボタンで対局へ
- **対局画面**: 盤面・両者の駒台・選択駒の移動先ハイライト・アクティブ能力ボタン・王手警告・面数/ボス表示・投了ボタン
- **報酬画面**: カード3枚(レアリティ枠色: コモン=木色/アンコモン=銀/レア=金/神話=紫)+スキップ
- **図鑑**: レアリティ別一覧。未遭遇はシルエット。詳細に動き図と能力
- **リザルト**: 勝利/敗北、到達面、今回のランの獲得駒一覧
- 駒は伝統的な五角形+漢字1〜2文字をCSSで描画。敵駒は180度回転。特殊駒はレアリティ色の縁取り。成り駒は赤字
- 表示言語は日本語のみ

## 10. テスト戦略

- `core/`は全てVitestでユニットテスト:
  - 通常駒8種+特殊駒32種+成り形+ボス3種の合法手生成(代表配置でスナップショット的に検証)
  - 各能力フックの裁定(§7のエッジケース全項目をテストケース化)
  - 成り(選択・強制)、持ち駒と打ち(二歩等)、勝敗判定(王取り・太子)、報酬抽選の確率境界
- AI健全性テスト: 王を1手で取れる局面で必ず取る/1手で取られる王を放置しない(深度2以上)
- UIは手動確認(開発サーバで実プレイ)

## 11. スコープ外(v1では作らない)

- 効果音・BGM
- スマホ最適化(デスクトップブラウザ優先。壊れない程度のレスポンシブのみ)
- 多言語対応、オンラインランキング、実績、リプレイ保存
- 初心者モード以外の難易度バリエーション(ハードモード等)
